apiVersion: apps/v1
kind: Deployment
metadata:
  name: nutriflow-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nutriflow-api
  template:
    metadata:
      labels:
        app: nutriflow-api
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: api
          image: nutriflow-api:local
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          envFrom:
            - configMapRef:
                name: nutriflow-api
            - secretRef:
                name: nutriflow-api-secrets
          startupProbe:
            httpGet:
              path: /actuator/health/liveness
              port: http
            failureThreshold: 30
            periodSeconds: 5
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: http
            periodSeconds: 5
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: http
            periodSeconds: 10
            timeoutSeconds: 3
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
